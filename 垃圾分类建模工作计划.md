# 垃圾图像分类建模工作计划

## 1. 工作目标与核心流程

本项目的核心任务是：将每张垃圾图像转换为固定长度的数值特征向量，经过特征筛选和特征融合后，输入传统机器学习分类模型，输出垃圾类别预测结果。

完整流程为：

```text
源数据选择
  -> 数据审查与分层划分
  -> 图像预处理
  -> 单图特征提取
  -> 特征矩阵构建
  -> 模型前特征筛选/降维
  -> 特征融合实验
  -> 分类模型训练与调参
  -> 模型后特征重要性分析
  -> 最终测试集评价
  -> 与已有深度学习研究结果对照
```

本项目重点呈现三件事：

1. 特征提取：每张图像提取一组定长特征向量。
2. 特征选择：包括模型前筛选和模型后重要性反馈。
3. 特征注入分类模型：将筛选后的特征向量输入 Logistic Regression、KNN、SVM、Decision Tree、Random Forest、XGBoost、LightGBM。

本项目不训练深度学习模型，也不提取 CNN embedding。深度学习方法只作为文献 benchmark，在最终讨论中与传统机器学习结果进行横向比较。

## 2. 源数据选择与数据版本

当前数据集目录为：

```text
data/garbage-classification-v2/
  original/
  standardized_256/
```

建模主数据选择 `standardized_256`，原因如下：

1. 所有图像已统一为 256×256，便于稳定提取定长特征。
2. 可以减少因原始尺寸差异带来的额外预处理误差。
3. 更适合传统机器学习流程中“图像 -> 特征向量 -> 分类器”的固定输入格式。

`original` 只用于必要的数据质量检查和报告示例，不作为正式建模输入。

当前类别与样本数如下：

| 类别 | 样本数 | 说明 |
|---|---:|---|
| clothes | 1,892 | 样本较多 |
| glass | 1,736 | 样本较多 |
| plastic | 1,597 | 样本较多 |
| shoes | 1,449 | 样本较多 |
| cardboard | 1,411 | 样本较多 |
| paper | 1,336 | 样本中等 |
| metal | 930 | 样本偏少 |
| battery | 756 | 样本偏少 |
| biological | 699 | 样本偏少 |
| trash | 453 | 样本最少 |

总样本量约为 12,259 张。由于类别不平衡明显，后续评价指标不能只看 accuracy，主指标采用 `macro-F1`，并重点观察 `trash`、`biological`、`battery` 等少数类的 recall。

## 3. 数据审查与分层划分

正式建模前先建立图像元数据表：

| 字段 | 含义 |
|---|---|
| image_id | 图像唯一编号 |
| path | 图像路径 |
| label | 类别标签 |
| width | 图像宽度 |
| height | 图像高度 |
| channels | 图像通道数 |
| split | train/validation/test |

数据审查步骤：

1. 检查所有图像是否可正常读取。
2. 检查图像尺寸是否均为 256×256。
3. 检查是否存在灰度图、透明通道图或损坏图。
4. 统计每类样本数并绘制类别分布柱状图。
5. 每类随机展示若干样本，观察背景复杂度、主体大小、光照、遮挡和类内差异。
6. 可选地用感知哈希检查近重复图像，但不轻易删除，除非重复明显影响训练/测试独立性。

数据划分策略：

| 数据集 | 比例 | 用途 |
|---|---:|---|
| train | 70% | 拟合特征选择器、PCA、k-means 视觉词典、训练模型 |
| validation | 15% | 模型筛选、特征组合比较、调参选择 |
| test | 15% | 最终一次性评价，不参与任何调参 |

划分要求：

1. 使用 `StratifiedShuffleSplit` 或等价方法进行分层抽样。
2. 固定随机种子，例如 `random_state=42`。
3. 保存划分结果到 `outputs/split_indices.json` 或 `outputs/metadata.csv`。
4. 所有模型、所有特征组合必须使用同一套划分，保证实验可比较。

## 4. 图像预处理

每张图像读取后生成以下基础版本：

| 版本 | 用途 |
|---|---|
| RGB 图 | 颜色统计、展示、部分直方图 |
| BGR 图 | OpenCV 颜色直方图 |
| HSV 图 | 颜色统计、颜色直方图 |
| Gray 灰度图 | LBP、GLCM、HOG、GIST、形状特征 |
| Binary/Mask 图 | 轮廓、Hu moments、形状特征 |

预处理步骤：

1. 读取图像并统一为 RGB 三通道。
2. 确保图像尺寸为 256×256。
3. 转换得到 HSV、BGR、Gray。
4. 对部分特征使用下采样图像，例如 HOG 使用 128×128 灰度图以控制维度。
5. 对形状特征使用 Otsu 阈值、Canny 边缘或最大轮廓方法得到主体区域。
6. 所有特征提取参数要固定并记录，避免不同实验不可复现。

注意：所有基于训练数据拟合的步骤，例如 `StandardScaler`、PCA、SelectKBest、RFE、k-means 视觉词典，只能在训练集上拟合，再应用到 validation/test，避免数据泄漏。

## 5. 特征提取总体设计

每张图像最终被表示为一行特征向量：

```text
image_i -> x_i = [f_color, f_texture, f_hog, f_shape, f_gist, f_bof]
```

所有图像堆叠得到：

```text
X: n_samples × n_features
y: n_samples × 1
```

特征提取分为七组：

| 编号 | 特征组 | 默认维度 | 主要表达内容 |
|---|---|---:|---|
| F0 | 原始像素 baseline | 1,024 或 3,072 | 直接像素强度 |
| F1 | 颜色特征 | 1,039 | 颜色分布、亮度、饱和度 |
| F2 | 纹理特征 | 30 | 表面纹理、粗糙度、局部模式 |
| F3 | HOG 边缘特征 | 1,764 | 边缘方向、局部轮廓 |
| F4 | 形状特征 | 12 | 面积、周长、长宽比、Hu moments |
| F5 | GIST 全局结构特征 | 64 | 全局空间布局、方向响应 |
| F6 | SIFT/ORB Bag of Features | 256 | 局部关键点视觉词频 |

默认全量手工特征维度为：

```text
F1 + F2 + F3 + F4 + F5 + F6
= 1039 + 30 + 1764 + 12 + 64 + 256
= 3165 维
```

## 6. 各类特征的具体提取方法

### 6.1 F0：原始像素 baseline

原始像素只作为最低基线，不作为最终主特征。

方案：

1. 灰度图下采样到 32×32，展平为 `32 × 32 = 1,024` 维。
2. RGB 图下采样到 32×32，展平为 `32 × 32 × 3 = 3,072` 维。
3. 像素值缩放到 `[0, 1]`。

用途：

1. 检查直接像素输入是否有基本分类能力。
2. 作为颜色、纹理、形状等手工特征的对照。

### 6.2 F1：颜色特征，默认 1,039 维

颜色特征由颜色统计量和颜色直方图组成。

颜色统计量：

1. 在 HSV 三个通道上分别计算 5 个统计量：mean、standard deviation、skewness、kurtosis、entropy。
2. 维度为 `3 × 5 = 15`。

颜色直方图：

1. 在 HSV 空间计算 3D 颜色直方图，默认 bins 为 `8 × 8 × 8 = 512`。
2. 在 BGR 空间计算 3D 颜色直方图，默认 bins 为 `8 × 8 × 8 = 512`。
3. 两个直方图拼接后维度为 `512 + 512 = 1,024`。

颜色特征总维度：

```text
HSV统计量 15 + HSV/BGR直方图 1024 = 1039 维
```

调参方案：

| 参数 | 候选值 | 对应维度变化 |
|---|---|---|
| histogram bins | 6×6×6 | 432 + 15 = 447 |
| histogram bins | 8×8×8 | 1,024 + 15 = 1,039 |
| histogram bins | 10×10×10 | 2,000 + 15 = 2,015 |

颜色特征预期贡献：

1. 区分 glass、plastic、biological 等颜色分布差异较明显的类别。
2. 捕捉纸张、纸板、衣物等亮度和饱和度差异。

### 6.3 F2：纹理特征，默认 30 维

纹理特征由 GLCM 和 LBP 组成。

GLCM 特征：

1. 将灰度图量化到固定灰度级，例如 32 或 64 级。
2. 默认距离 `distance=1`。
3. 默认角度为 `0°、45°、90°、135°`，共 4 个方向。
4. 每个方向计算 5 个统计量：contrast、dissimilarity、homogeneity、energy、correlation。
5. 维度为 `4 × 5 = 20`。

LBP 特征：

1. 使用 uniform LBP。
2. 默认 `P=8` 个邻域采样点，`R=1`。
3. uniform LBP 的直方图 bins 为 `P + 2 = 10`。
4. 维度为 10。

纹理特征总维度：

```text
GLCM 20 + LBP 10 = 30 维
```

调参方案：

| 特征 | 参数 | 候选值 | 维度变化 |
|---|---|---|---|
| GLCM | distance | 1 / 2 / 4 | 每增加一个 distance 增加 20 维 |
| GLCM | gray levels | 32 / 64 | 主要影响统计稳定性 |
| LBP | P, R | P=8,R=1 | 10 维 |
| LBP | P, R | P=16,R=2 | 18 维 |

纹理特征预期贡献：

1. 区分 paper、cardboard、clothes 的表面纹理。
2. 区分 metal、plastic、glass 的光滑度和局部灰度结构。

### 6.4 F3：HOG 边缘特征，默认 1,764 维

HOG 用于描述局部梯度方向和边缘结构。

默认设置：

1. 将灰度图下采样到 128×128。
2. `orientations=9`。
3. `pixels_per_cell=(16,16)`。
4. `cells_per_block=(2,2)`。
5. 128×128 图像被划分为 `8 × 8` 个 cell。
6. block 数为 `(8-1) × (8-1) = 7 × 7`。
7. 每个 block 维度为 `2 × 2 × 9 = 36`。

HOG 特征总维度：

```text
7 × 7 × 2 × 2 × 9 = 1764 维
```

调参方案：

| 参数 | 候选值 | 维度影响 |
|---|---|---|
| orientations | 8 / 9 / 12 | 方向数越多，维度越高 |
| pixels_per_cell | 16×16 | 默认 1,764 维 |
| pixels_per_cell | 8×8 | 约 8,100 维，表达更细但更容易过拟合 |
| image size | 128×128 / 256×256 | 图像越大，HOG 维度越高 |

HOG 特征预期贡献：

1. 区分 battery、shoes、cardboard 等轮廓较稳定的类别。
2. 弥补颜色和纹理无法表达整体边缘结构的问题。

### 6.5 F4：形状特征，默认 12 维

形状特征依赖主体区域提取。

主体提取步骤：

1. 灰度化。
2. Otsu 阈值或 Canny 边缘检测。
3. 查找轮廓。
4. 选择最大轮廓作为主体近似区域。
5. 若没有可靠轮廓，则该图形状特征填 0，并记录失败标记。

轮廓几何特征：

| 特征 | 含义 |
|---|---|
| area | 最大轮廓面积 |
| perimeter | 最大轮廓周长 |
| aspect_ratio | 外接矩形宽高比 |
| extent | 面积 / 外接矩形面积 |
| solidity | 面积 / 凸包面积 |

轮廓特征维度为 5。

Hu moments：

1. 在主体 mask 或灰度图上计算 7 个 Hu 不变矩。
2. Hu moments 对平移、尺度、旋转具有一定不变性。

形状特征总维度：

```text
轮廓几何 5 + Hu moments 7 = 12 维
```

形状特征预期贡献：

1. 区分电池、鞋、纸盒、瓶状物等几何差异。
2. 提供低维、可解释特征。

### 6.6 F5：GIST 全局结构特征，默认 64 维

GIST 用于描述图像的全局空间结构，不关注少数关键点，而是用 Gabor 滤波器捕捉整体方向响应。

默认简化版 GIST 设置：

1. 输入 128×128 灰度图。
2. 将图像划分为 `4 × 4 = 16` 个网格。
3. 使用 4 个方向的 Gabor 滤波器。
4. 每个网格统计 4 个方向响应的均值。

GIST 默认维度：

```text
4 × 4 × 4 = 64 维
```

可选增强版：

1. 使用多尺度 Gabor 滤波器。
2. 方向数设置为 `[8, 8, 4, 4]`，总方向响应数为 24。
3. 仍使用 4×4 网格。

增强版 GIST 维度：

```text
4 × 4 × 24 = 384 维
```

本项目默认先使用 64 维简化版 GIST。如果单独 GIST 表现较好，再比较 384 维增强版。

GIST 特征预期贡献：

1. 捕捉图像整体布局、主体大致位置和方向分布。
2. 补充 HOG 的局部边缘特征。
3. 对背景复杂度和全局结构差异可能有一定表达能力。

### 6.7 F6：SIFT/ORB Bag of Features，默认 256 维

SIFT 和 ORB 每张图的关键点数量不同，不能直接拼接成固定长度向量，因此使用 Bag of Features。

训练视觉词典步骤：

1. 只在训练集上提取 SIFT/ORB 描述子。
2. 汇总训练集所有描述子。
3. 使用 k-means 聚类生成视觉词典。
4. 默认 SIFT 视觉词数量 `k=128`。
5. 默认 ORB 视觉词数量 `k=128`。

单图向量化步骤：

1. 对一张图提取 SIFT/ORB 描述子。
2. 将每个描述子分配到最近的视觉词。
3. 统计视觉词频直方图。
4. 对直方图做 L1 或 L2 归一化。

默认维度：

```text
SIFT-BoF 128 + ORB-BoF 128 = 256 维
```

调参方案：

| 视觉词数量 | 单一描述子维度 | SIFT+ORB 合并维度 |
|---:|---:|---:|
| 64 | 64 | 128 |
| 128 | 128 | 256 |
| 256 | 256 | 512 |

注意：

1. k-means 视觉词典只能在训练集描述子上拟合。
2. validation/test 只能使用训练集拟合好的视觉词典进行转换。
3. 如果某张图没有检测到关键点，则对应 BoF 向量填 0。

## 7. 特征矩阵与特征命名

所有特征提取后保存为可复用缓存：

```text
outputs/features/
  F0_pixels_gray_1024.npz
  F1_color_1039.npz
  F2_texture_30.npz
  F3_hog_1764.npz
  F4_shape_12.npz
  F5_gist_64.npz
  F6_bof_256.npz
```

每个缓存文件应包含：

1. `X_train`, `X_val`, `X_test`
2. `y_train`, `y_val`, `y_test`
3. `feature_names`
4. `feature_groups`
5. `params`

特征列命名示例：

```text
color_hsv_mean_H
color_hsv_std_S
texture_glcm_contrast_0deg
texture_lbp_bin_03
hog_0001
shape_area
gist_cell_02_dir_1
bof_sift_word_027
```

这样做的好处是：后续可以按特征组统计重要性，也能在报告中解释哪些特征贡献较大。

## 8. 特征组合与融合方案

先做单特征组实验，再做融合实验。

单特征组：

| 组合编号 | 内容 | 维度 |
|---|---|---:|
| C0 | 灰度像素 baseline | 1,024 |
| C1 | 颜色特征 | 1,039 |
| C2 | 纹理特征 | 30 |
| C3 | HOG 特征 | 1,764 |
| C4 | 形状特征 | 12 |
| C5 | GIST 特征 | 64 |
| C6 | SIFT/ORB BoF | 256 |

参考融合特征组：

| 组合编号 | 内容 | 维度 |
|---|---|---:|
| C7 | 颜色 + 纹理 | 1,069 |
| C8 | 颜色 + 纹理 + 形状 | 1,081 |
| C9 | 颜色 + 纹理 + 形状 + GIST | 1,145 |
| C10 | 颜色 + 纹理 + 形状 + GIST + HOG | 2,909 |
| C11 | 颜色 + 纹理 + 形状 + GIST + HOG + BoF | 3,165 |

融合策略：

阶段三的融合方案不预先固定。C7-C11 只是参考候选，用来代表常见的轻量融合、结构增强融合和全量融合；真正进入阶段三的组合要依赖阶段二单特征验证结果。

1. 首先比较 C1-C6 单特征组的 validation macro-F1、少数类 recall 和训练时间。
2. 保留三类候选特征组：
   - 单独表现好的特征组，例如 macro-F1 排名前列的颜色、HOG 或 BoF。
   - 对少数类有帮助的特征组，例如整体分数一般但能提升 `trash`、`battery`、`biological` recall 的 GIST 或形状特征。
   - 与其他特征互补的特征组，例如纹理改善 paper/cardboard，形状改善 battery/shoes。
3. 选出阶段二中表现最好的单特征组作为起点，记为 `best_single`。
4. 采用逐步加法融合，每次只加入一个候选特征组：

```text
best_single
best_single + feature_A
best_single + feature_B
best_single + feature_C
best_single + feature_A + feature_B
best_single + feature_A + feature_C
...
```

5. 每次加入新特征组后，都重新训练同一批候选模型，并比较 validation macro-F1、少数类 recall 和训练时间。
6. 如果加入某组特征后 macro-F1 提升，或者少数类 recall 明显提升，则保留该特征组。
7. 如果加入某组特征后性能下降、训练时间明显增加且少数类无改善，则暂不纳入最终融合。
8. 最终融合组合由实验结果决定，不必固定为 C9、C10 或 C11。可能的最终组合包括但不限于：
   - 颜色 + 纹理 + GIST
   - 颜色 + HOG + BoF
   - HOG + GIST + XGBoost
   - 颜色 + 纹理 + 形状 + LightGBM
9. C9、C10、C11 仍作为参考组合保留，用于和自适应融合结果比较，判断“全量特征是否真的优于筛选后的组合”。

最终必须做特征组消融实验：

| 消融实验 | 内容 |
|---|---|
| A0 | 最终融合特征 |
| A1 | 从最终融合中去掉颜色 |
| A2 | 从最终融合中去掉纹理 |
| A3 | 从最终融合中去掉形状 |
| A4 | 从最终融合中去掉 GIST |
| A5 | 从最终融合中去掉 HOG |
| A6 | 从最终融合中去掉 BoF |

消融实验用于回答：哪些特征真正有贡献，哪些特征只是增加维度但没有提升分类效果。

## 9. 模型前特征选择与降维

模型前特征选择是在训练分类模型之前进行的筛选或变换。所有步骤只能在训练集上拟合。

### 9.1 低方差过滤

目的：删除几乎不变化的特征。

步骤：

1. 在训练集 `X_train` 上计算每一列方差。
2. 删除方差低于阈值的列。
3. 将同样的列选择规则应用到 `X_val` 和 `X_test`。

候选阈值：

```text
0
0.0001
0.001
0.01
```

适用特征：

1. 形状特征中可能出现大量 0 值。
2. BoF 中可能存在很少出现的视觉词。
3. 高维颜色直方图中可能有长期为空的 bins。

### 9.2 相关性过滤

目的：删除高度重复的特征。

步骤：

1. 在训练集上计算特征两两 Pearson 相关系数。
2. 如果两个特征满足 `|corr| > 0.90` 或 `|corr| > 0.95`，认为它们高度冗余。
3. 优先保留单特征实验中贡献更高、解释性更强或缺失更少的特征。
4. 保存保留下来的列索引，并应用到 validation/test。

候选阈值：

```text
0.90
0.95
0.98
```

注意：

1. 相关性过滤适合颜色统计、纹理统计、形状统计等可解释特征。
2. 对 HOG、BoF 这类高维局部特征，可先不做全量相关性计算，避免计算成本过高。

### 9.3 SelectKBest

目的：根据单变量统计关系筛选与类别最相关的特征。

方法：

1. 使用 `f_classif` 或 `mutual_info_classif`。
2. 在训练集上计算每一列特征与类别标签的关系。
3. 保留得分最高的前 k 个特征。

候选 k：

```text
100
300
500
1000
1500
```

适用模型：

1. Logistic Regression
2. KNN
3. SVM

### 9.4 PCA 降维

PCA 严格说是特征变换，不是选择原始列，但在本项目中作为广义特征选择/降维方法使用。

步骤：

1. 先用 `StandardScaler` 在训练集上标准化。
2. 在训练集上拟合 PCA。
3. 将训练集、验证集、测试集投影到相同主成分空间。

候选保留方差比例：

```text
85%
90%
95%
```

候选固定维度：

```text
100
200
300
500
```

重点适用模型：

1. KNN：缓解高维距离失效。
2. Logistic Regression：减少冗余，提高稳定性。
3. SVM：降低训练成本。

树模型、Random Forest、XGBoost、LightGBM 不优先使用 PCA，因为 PCA 会破坏原始特征解释性，且树模型本身能处理非线性和部分冗余。

### 9.5 Wrapper 方法

Wrapper 方法通过反复训练模型来筛选特征。

候选方法：

1. RFE：递归特征消除。
2. RFECV：带交叉验证的递归特征消除。

候选筛选器：

1. Logistic Regression
2. Linear SVM
3. Random Forest

操作步骤：

1. 先选择一个候选融合特征，例如 C9 或 C10。
2. 使用较轻量模型作为 estimator。
3. 每轮删除贡献较小的一部分特征。
4. 在 validation 或交叉验证上选择最佳特征数量。

注意：

1. Wrapper 计算成本高，不对所有特征组合都使用。
2. 优先用于 C9 轻量融合特征，再视情况用于 C10。

### 9.6 Embedded 方法

Embedded 方法在模型训练过程中完成特征选择。

候选方法：

1. L1 Logistic Regression：利用 L1 正则将部分系数压缩为 0。
2. Random Forest importance：根据树分裂贡献筛选。
3. XGBoost importance：根据 gain、split、cover 等指标筛选。
4. LightGBM importance：根据 split 或 gain 筛选。
5. `SelectFromModel`：根据模型重要性阈值选择特征。

候选阈值：

```text
mean
median
top 20%
top 30%
top 50%
```

操作步骤：

1. 在训练集上训练嵌入式筛选模型。
2. 根据重要性或系数绝对值排序。
3. 选择重要性高的特征列。
4. 使用被选择的特征重新训练七类分类模型。
5. 比较筛选前后 validation macro-F1 和训练时间。

## 10. 模型后特征重要性与反馈筛选

模型后筛选不是第一次训练前的预处理，而是训练模型后对特征贡献进行解释，再反馈到下一轮特征组合。

### 10.1 系数重要性

适用模型：

1. Logistic Regression
2. Linear SVM

方法：

1. 使用标准化后的特征训练模型。
2. 查看 `coef_` 的绝对值。
3. 将特征重要性按特征组汇总：
   - color
   - texture
   - shape
   - gist
   - hog
   - bof

### 10.2 树模型重要性

适用模型：

1. Decision Tree
2. Random Forest
3. XGBoost
4. LightGBM

方法：

1. 提取 `feature_importances_` 或 gain/split importance。
2. 统计单个特征的重要性排名。
3. 按特征组求和，得到组级贡献。
4. 判断 GIST、HOG、BoF 等高维特征是否真正贡献性能。

### 10.3 Permutation Importance

Permutation importance 更适合作为最终报告中的解释方法。

步骤：

1. 在 validation 集上计算原始 macro-F1。
2. 随机打乱某个特征或某个特征组。
3. 重新预测并计算 macro-F1 下降幅度。
4. 下降越大，说明该特征或特征组越重要。

组级 permutation 方案：

| 特征组 | 打乱列范围 |
|---|---|
| color | 1,039 列 |
| texture | 30 列 |
| shape | 12 列 |
| gist | 64 列 |
| hog | 1,764 列 |
| bof | 256 列 |

模型后分析的反馈规则：

1. 如果某特征组重要性长期接近 0，则在下一轮融合中删除。
2. 如果某特征组只提升少数类 recall，即使整体 macro-F1 提升不大，也保留并在报告中说明。
3. 如果 GIST 提供较高组级重要性，说明全局结构信息有效；如果 GIST 贡献低，则说明本数据更依赖局部纹理、颜色或边缘。

## 11. 分类模型设计

分类模型分为四个基线模型和三个固定集成模型。

### 11.1 四大基线模型

| 模型 | 输入 | 输出 | 作用 |
|---|---|---|---|
| Logistic Regression | 筛选后的特征向量 | 10 类概率/类别 | 线性可解释基线 |
| KNN | 筛选后的特征向量 | 10 类类别 | 检验特征空间近邻结构 |
| SVM | 筛选后的特征向量 | 10 类类别 | 传统图像分类强基线 |
| Decision Tree | 筛选后的特征向量 | 10 类类别 | 可解释树模型基线 |

### 11.2 三大固定集成模型

| 模型 | 输入 | 输出 | 作用 |
|---|---|---|---|
| Random Forest | 筛选后的特征向量 | 10 类类别 | Bagging 集成，稳定、可解释 |
| XGBoost | 筛选后的特征向量 | 10 类类别 | Boosting 强模型，处理非线性特征交互 |
| LightGBM | 筛选后的特征向量 | 10 类类别 | 高效 Boosting 强模型，适合结构化特征 |

所有分类模型都直接接收特征矩阵 `X` 和标签 `y`：

```text
X_selected -> classifier.fit(X_train, y_train)
X_selected -> classifier.predict(X_val/test)
```

模型前处理要求：

| 模型 | 是否需要 StandardScaler | 是否优先使用 PCA/筛选 |
|---|---|---|
| Logistic Regression | 是 | 是 |
| KNN | 是 | 强烈建议 |
| SVM | 是 | 是 |
| Decision Tree | 否 | 可选 |
| Random Forest | 否 | 可选 |
| XGBoost | 否 | 可选 |
| LightGBM | 否 | 可选 |

## 12. 调参计划

调参分为特征参数调参、特征选择调参、模型参数调参三层。

### 12.1 特征参数调参

| 特征组 | 参数 | 候选值 | 关注点 |
|---|---|---|---|
| 颜色直方图 | bins | 6 / 8 / 10 | 颜色表达与维度平衡 |
| GLCM | gray levels | 32 / 64 | 纹理稳定性 |
| GLCM | distance | 1 / 1+2 / 1+2+4 | 多尺度纹理 |
| LBP | P,R | 8,1 / 16,2 | 局部纹理粒度 |
| HOG | orientations | 8 / 9 / 12 | 边缘方向细度 |
| HOG | pixels_per_cell | 16 / 8 | 维度与细节 |
| GIST | filters | 4方向 / 24响应 | 64 维 vs 384 维 |
| BoF | k | 64 / 128 / 256 | 视觉词表达能力 |

调参原则：

1. 先使用默认维度建立完整 pipeline。
2. 如果某特征组单独表现较好，再对该组做细调。
3. 如果高维版本带来明显过拟合或耗时增加，则回退到低维版本。

### 12.2 特征选择参数调参

| 方法 | 参数 | 候选值 |
|---|---|---|
| VarianceThreshold | threshold | 0 / 0.0001 / 0.001 / 0.01 |
| Correlation Filter | corr threshold | 0.90 / 0.95 / 0.98 |
| SelectKBest | k | 100 / 300 / 500 / 1000 / 1500 |
| PCA | explained variance | 0.85 / 0.90 / 0.95 |
| PCA | fixed components | 100 / 200 / 300 / 500 |
| SelectFromModel | threshold | mean / median / top 20% / top 30% / top 50% |

比较指标：

1. validation macro-F1。
2. 少数类 recall。
3. 特征维度减少比例。
4. 训练时间变化。

### 12.3 模型参数调参

Logistic Regression：

```text
C: 0.01 / 0.1 / 1 / 10
penalty: l2 / l1
solver: lbfgs / saga
class_weight: None / balanced
```

KNN：

```text
n_neighbors: 3 / 5 / 7 / 11 / 15 / 21
weights: uniform / distance
metric: euclidean / manhattan
```

SVM：

```text
kernel: linear / rbf
C: 0.1 / 1 / 10 / 100
gamma: scale / 0.001 / 0.01 / 0.1
class_weight: None / balanced
```

Decision Tree：

```text
criterion: gini / entropy
max_depth: 5 / 10 / 20 / None
min_samples_split: 2 / 5 / 10
min_samples_leaf: 1 / 3 / 5 / 10
```

Random Forest：

```text
n_estimators: 300 / 500
max_depth: 10 / 20 / None
max_features: sqrt / log2
min_samples_leaf: 1 / 3 / 5
class_weight: None / balanced
```

XGBoost：

```text
n_estimators: 300 / 500 / 800
learning_rate: 0.03 / 0.05 / 0.1
max_depth: 3 / 5 / 7
subsample: 0.8 / 1.0
colsample_bytree: 0.8 / 1.0
reg_alpha: 0 / 0.1 / 1
reg_lambda: 1 / 5 / 10
```

LightGBM：

```text
n_estimators: 300 / 500 / 800
learning_rate: 0.03 / 0.05 / 0.1
num_leaves: 31 / 63 / 127
max_depth: -1 / 10 / 20
feature_fraction: 0.8 / 1.0
bagging_fraction: 0.8 / 1.0
min_child_samples: 10 / 20 / 50
```

调参方法：

1. 广筛阶段使用默认参数或小网格。
2. 精调阶段使用 `GridSearchCV` 或 `RandomizedSearchCV`。
3. 主评分指标为 `f1_macro`。
4. 调参只在 train/validation 或 train 内部交叉验证上进行。
5. test 集只在最终模型确定后使用一次。

## 13. 实验阶段安排

### 阶段一：数据和特征可用性检查

目标：

1. 确认图像都能读取。
2. 确认每组特征都能成功提取。
3. 记录每组特征的维度、耗时、失败样本数。

输出：

```text
metadata.csv
class_distribution.png
feature_extraction_report.csv
```

### 阶段二：单特征组 + 基线模型

实验：

1. C0-C6 分别输入四个基线模型。
2. 比较不同特征组的基本分类能力。
3. 记录 validation macro-F1、accuracy、weighted-F1、少数类 recall。

目的：

1. 判断颜色、纹理、HOG、形状、GIST、BoF 哪些特征有效。
2. 判断哪些特征组进入融合实验。

### 阶段三：融合特征 + 七个模型广筛

实验：

1. 根据阶段二结果确定候选特征组，而不是机械固定使用 C7-C11。
2. 从阶段二最优单特征组出发，采用逐步加法构造融合特征。
3. 每产生一个新融合组合，就输入七个模型进行广筛。
4. C7-C11 作为参考组合保留，用于比较自适应融合是否优于固定融合路线。
5. 加入低方差过滤和标准化。
6. 对 Logistic、KNN、SVM 比较 PCA/SelectKBest。
7. 对 RF、XGBoost、LightGBM 比较是否使用 Embedded 筛选。

目的：

1. 找到表现最好的特征组合。
2. 找到最值得精调的模型。
3. 判断全量特征是否必要，还是筛选后的少数组合更稳健。

### 阶段四：精调

阶段四的精调必须依赖阶段三结果，不预先固定为 C9/C10/C11 或某几个模型。阶段三会产生若干组“融合特征 + 分类模型 + 初步筛选方法”的候选组合，阶段四只对其中最有价值的组合进行细调。

进入精调的候选选择规则：

1. 按 validation macro-F1 排序，选择排名前 3-5 的组合。
2. 如果某组合整体 macro-F1 不是最高，但显著提升 `trash`、`battery`、`biological` 等少数类 recall，也进入精调候选。
3. 如果两个组合分数接近，优先保留维度更低、训练更快、解释性更好的组合。
4. 四大基线模型中至少保留一个表现最好的模型，用于和集成模型比较。
5. 三大集成模型 Random Forest、XGBoost、LightGBM 若阶段三表现接近，则都进入精调；若某个模型明显落后且耗时高，可只保留为广筛结果，不再精调。

可能进入精调的组合示例包括但不限于：

1. `阶段三最佳融合特征 + SVM`
2. `阶段三最佳融合特征 + Random Forest`
3. `阶段三最佳融合特征 + XGBoost`
4. `阶段三最佳融合特征 + LightGBM`
5. `少数类 recall 最优融合特征 + 对应模型`
6. `低维高效融合特征 + 最佳基线模型`

精调重点：

1. 特征选择参数。
2. 模型超参数。
3. 类别不平衡处理。

精调顺序：

1. 固定阶段三选出的融合特征组合，先调模型超参数。
2. 在模型参数较稳定后，再细调特征选择参数，例如 SelectKBest 的 k、PCA 维度、Embedded 重要性阈值。
3. 最后比较类别不平衡处理方案，例如 `class_weight`、sample weight、少数类增强。
4. 每次只改变一类因素，避免无法判断性能提升来自模型、特征选择还是类别权重。
5. 使用 validation macro-F1 作为主选择标准，同时记录少数类 recall 和训练时间。
6. test 集仍然封存，不能参与阶段四精调。

类别不平衡处理方案：

1. `class_weight='balanced'`。
2. sample weight。
3. 少数类训练增强。
4. macro-F1 作为调参目标。

### 阶段五：模型后解释与二次筛选

步骤：

1. 对最佳模型计算特征重要性。
2. 按特征组汇总贡献。
3. 做 permutation importance。
4. 做特征组消融实验。
5. 根据结果重新训练简化模型。

目标：

1. 得到不仅分数高、而且解释清楚的模型。
2. 为报告提供“中间思考过程”和“优化过程”。

### 阶段六：最终测试集评价

最终模型确定后，在 test 集上只评价一次。

输出指标：

1. accuracy
2. macro precision
3. macro recall
4. macro-F1
5. weighted-F1
6. 每类 precision/recall/F1
7. confusion matrix
8. 少数类 recall
9. 训练时间和预测时间

最终必须报告混淆矩阵，并重点分析：

1. 哪些类别最容易混淆。
2. `trash` 是否被误判为其他类别。
3. `battery`、`biological` 等少数类表现是否稳定。
4. GIST、HOG、BoF 等高维特征是否真正提升最终结果。

## 14. 最终结果评价与文献对照

本项目最终模型只从传统机器学习模型中选择：

1. Logistic Regression
2. KNN
3. SVM
4. Decision Tree
5. Random Forest
6. XGBoost
7. LightGBM

最终选择标准：

1. test macro-F1 最高或接近最高。
2. 少数类 recall 不低。
3. validation 到 test 的性能下降不大。
4. 训练时间和预测时间可接受。
5. 特征贡献可以解释。
6. 有完整的筛选、调参、消融证据支持。

与深度学习文献对照时说明：

1. 文献中的 EfficientNetV2、ResNet、MobileNet 等属于端到端深度学习模型。
2. 本项目不复现深度模型，只引用已有 benchmark。
3. 对比重点不是证明传统方法一定超过深度学习，而是说明：
   - 传统方法的准确率上限；
   - 手工特征的解释性；
   - 特征筛选对训练效率的影响；
   - 集成学习相比四个基线模型的提升。

## 15. 实验记录模板

每次实验都记录如下信息：

| Exp ID | 特征组合 | 原始维度 | 筛选方法 | 筛选后维度 | 模型 | 关键参数 | Val Macro-F1 | Test Macro-F1 | 少数类表现 | 结论 |
|---|---|---:|---|---:|---|---|---:|---:|---|---|
| E00 | C0 pixels | 1024 | none | 1024 | Logistic | C=1 |  |  |  | 像素基线 |
| E01 | C1 color | 1039 | scaler | 1039 | SVM | C/gamma |  |  |  | 颜色贡献 |
| E02 | C2 texture | 30 | scaler | 30 | RF | depth/trees |  |  |  | 纹理贡献 |
| E03 | C5 GIST | 64 | scaler | 64 | SVM | C/gamma |  |  |  | 全局结构贡献 |
| E04 | C9 light fusion | 1145 | variance+corr |  | XGBoost | lr/depth |  |  |  | 轻量融合 |
| E05 | C10 + HOG | 2909 | PCA/SelectKBest |  | SVM | C/gamma |  |  |  | 边缘增强 |
| E06 | C11 full | 3165 | Embedded |  | LightGBM | leaves/lr |  |  |  | 全量融合 |
| E07 | C11 ablation | 3165 | remove GIST | 3101 | best model | fixed |  |  |  | GIST 消融 |

## 16. 大作业正文建议结构

最终报告建议按以下顺序写：

1. 研究问题与意义。
2. 数据来源、类别说明和样本分布。
3. 数据预处理和训练/验证/测试划分。
4. 特征提取方法：颜色、纹理、HOG、形状、GIST、BoF，并写清楚维度。
5. 特征选择方法：低方差、相关性、PCA、SelectKBest、Wrapper、Embedded。
6. 分类模型设计：四大基线模型 + 三大集成模型。
7. 实验过程：单特征实验、融合实验、模型广筛、调参。
8. 模型后解释：特征重要性、permutation importance、消融实验。
9. 最终测试集结果：指标表、混淆矩阵、错误案例。
10. 与深度学习文献结果对照。
11. 研究结论、不足与改进方向。

## 17. 参考文献与依据

1. Yang, M., & Thung, G. (2016). Classification of Trash for Recyclability Status.
2. Nguyen, N.-B.-Q., Do, T.-M., Phan, C.-T., & Phan, T.-T.-H. (2025/2026). Towards Accurate and Efficient Waste Image Classification: A Hybrid Deep Learning and Machine Learning Approach.
3. Kunwar, S. (2026). The Garbage Dataset (GD): A Multi-Class Image Benchmark for Automated Waste Segregation.
4. Mboli, J. S., & Ogungbemi, O. A. (2026). AI-Enabled Waste Classification as a Data-Driven Decision Support Tool for Circular Economy and Urban Sustainability.
5. Sen, A., Maiti, G., Parida, B. K., Mishra, B. P., Arya, M., & Bondar, D. I. (2025/2026). Feature Engineering is Not Dead: Reviving Classical Machine Learning with Entropy, HOG, and LBP Feature Fusion for Image Classification.
